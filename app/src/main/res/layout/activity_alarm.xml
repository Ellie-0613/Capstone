<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ContentActivity">


    <ImageView
        android:id="@+id/sonaji"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_gravity="center"
        android:layout_marginTop="100dp"
        android:background="@null"
        android:src="@drawable/main"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <Button
        android:id="@+id/alarm"
        android:layout_width="350dp"
        android:layout_height="70dp"
        android:layout_marginTop="150dp"
        android:ems="100"
        android:gravity="center"
        android:text="낙상 알림 보내기"
        android:textColor="@color/black"
        android:textSize="35sp"
        app:backgroundTint="#FFCECE"
        app:layout_constraintEnd_toEndOf="@+id/sonaji"
        app:layout_constraintStart_toStartOf="@+id/sonaji"
        app:layout_constraintTop_toTopOf="@+id/sonaji" />
    <Button
        android:id="@+id/logout"
        android:layout_width="350dp"
        android:layout_height="70dp"
        android:layout_marginTop="80dp"
        android:gravity="center"
        android:ems="100"
        android:text="로그아웃"
        android:textColor="@color/black"
        android:textSize="35sp"
        app:backgroundTint="#FFCECE"
        app:layout_constraintEnd_toEndOf="@+id/alarm"
        app:layout_constraintStart_toStartOf="@+id/alarm"
        app:layout_constraintTop_toTopOf="@+id/alarm"
        />


</androidx.constraintlayout.widget.ConstraintLayout>